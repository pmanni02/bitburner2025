// import { NS } from "../NetscriptDefinitions.d.ts"
// import { addNewServer } from '../src/utils/servers.ts'

describe('Server helper functions', () => {
  describe('addNewServer', () => {
    test('it should add a new server to an existing config', () => {
      // const config = {
      //   growServers: ["n00dles", "sigma-cosmetics"],
      //   hackServers: [],
      //   weakenServers: ["joesguns"],
      //   targetServer: "foodnstuff"
      // }
      // const updatedConfig = addNewServer(NS, config)
      expect(1).toBe(1)
    })
  })
})